using Actions;

namespace Effects.Base
{
    /// <summary>Not currently used. Previously held a GameAction output from a resolved effect.</summary>
    public struct EffectResult
    {
        public GameAction Action;
    }
}
